node_modules/
dist/
dist-*/
.wrangler/

# The local server keeps its SQLite database and image bucket here. Trading
# data, never committed.
data/

# Runtime dirs on the box: the served app bundles and the nightly snapshots.
web/
backups/

# Secrets and environment files
.env
.env.*
.dev.vars
*.pem
*.key
.secrets*
